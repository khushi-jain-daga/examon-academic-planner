(()=>{'use strict';
const LOGO='assets/examon-logo.webp',KEY='examonPlannerStableV2';
const FACULTY=[['f-gaurav','Gaurav Sir','Mechanical • Quantitative Aptitude'],['f-shivam','Shivam Sir','Mechanical • Reasoning'],['f-amrit','Amrit Sir','Non-Technical'],['f-anjna','Anjna Ma’am','General Awareness • Science'],['f-joshit','Joshit Sir','Physics • Civil'],['f-vivek','vivek sir','Mechanical'],['f-rakhi','Rakhi Ma’am','English']].map(x=>({id:x[0],name:x[1],domain:x[2]}));
const SUBJECTS=[
['quant','Quantitative Aptitude','Non-Tech','Non-Technical Theory',['f-gaurav'],10,'2026-09-14','12:30','Non-Tech Quant'],
['reasoning','Reasoning','Non-Tech','Non-Technical Theory',['f-shivam'],10,'2026-09-11','04:30','Non-Tech Reasoning'],
['history','History','Non-Tech','Non-Technical Theory',['f-amrit'],10,'2026-10-05','21:30','Non-Tech History'],
['polity','Polity','Non-Tech','Non-Technical Theory',['f-amrit'],10,'2026-09-11','21:30','Non-Tech Polity'],
['geography','Geography','Non-Tech','Non-Technical Theory',['f-amrit'],10,'2026-09-11','21:30','Non-Tech Geography'],
['chemistry','Chemistry','Non-Tech','Non-Technical Theory',['f-anjna'],10,'2026-10-05','10:15','Non-Tech Science'],
['biology','Biology','Non-Tech','Non-Technical Theory',['f-anjna'],10,'2026-09-16','10:15','Non-Tech Science'],
['economy','Economy','Non-Tech','Non-Technical Theory',['f-anjna'],10,'2026-10-19','10:15','Non-Tech Science'],
['physics','Physics','Non-Tech','Non-Technical Theory',['f-joshit'],10,'2026-10-05','03:30','Non-Tech Science'],
['static-gk','Static GK / Current Affairs','Non-Tech','Non-Technical Theory',['f-anjna'],10,'2026-09-21','11:15','Non-Tech GA'],
['nontech-practice','Non-Technical Practice','Non-Tech','Non-Technical Practice',['f-amrit','f-anjna'],30,'2026-09-21','19:00','Non-Tech Practice'],
['eng-mech','Engineering Mechanics','Mechanical','Technical Theory',['f-gaurav'],10,'2026-09-11','12:30','Technical Theory A'],
['production','Production Engineering','Mechanical','Technical Theory',['f-gaurav'],10,'2026-09-14','12:30','Technical Theory A'],
['tom','Theory of Machines','Mechanical','Technical Theory',['f-gaurav'],15,'2026-10-15','12:30','Technical Theory A'],
['machine-design','Machine Design','Mechanical','Technical Theory',['f-gaurav'],10,'2026-10-30','12:30','Technical Theory A'],
['material-science','Material Science','Mechanical','Technical Theory',['f-gaurav','f-shivam'],10,'2026-09-21','12:30','Technical Theory B'],
['eng-maths','Engineering Mathematics','Mechanical','Technical Theory',['f-gaurav','f-shivam'],10,'2026-09-22','12:30','Technical Theory B'],
['fluid','Fluid Mechanics','Mechanical','Technical Theory',['f-shivam'],10,'2026-09-16','12:30','Technical Theory B'],
['hydraulic','Hydraulic Machines','Mechanical','Technical Theory',['f-shivam'],8,'2026-09-30','12:30','Technical Theory B'],
['thermo','Thermodynamics','Mechanical','Technical Theory',['f-shivam'],10,'2026-09-28','16:00','Technical Theory B'],
['heat-transfer','Heat Transfer','Mechanical','Technical Theory',['f-gaurav'],10,'2026-10-12','12:30','Technical Theory A'],
['rac','RAC','Mechanical','Technical Theory',['f-shivam'],7,'2026-10-01','13:00','Technical Theory B'],
['ic-engine','IC Engine','Mechanical','Technical Theory',['f-shivam'],10,'2026-10-08','16:00','Technical Theory B'],
['power-plant','Power Plant Engineering','Mechanical','Technical Theory',['f-shivam'],10,'2026-10-20','16:00','Technical Theory B'],
['som','Strength of Materials','Mechanical','Technical Theory',['f-shivam'],10,'2026-09-18','12:30','Technical Theory B'],
['industrial','Industrial Engineering','Mechanical','Technical Theory',['f-shivam'],10,'2026-11-02','12:30','Technical Theory B'],
['manufacturing','Manufacturing Processes','Mechanical','Technical Theory',['f-gaurav'],10,'2026-11-16','12:30','Technical Theory A'],
['welding','Welding','Mechanical','Technical Theory',['f-gaurav'],5,'2026-11-23','12:30','Technical Theory A'],
['metal-casting','Metal Casting','Mechanical','Technical Practice',['f-gaurav','f-shivam'],5,'2026-09-03','16:00','Technical Practice'],
['forming','Forming Processes','Mechanical','Technical Practice',['f-gaurav','f-shivam'],5,'2026-09-10','16:00','Technical Practice'],
['machining','Machining Processes','Mechanical','Technical Practice',['f-gaurav','f-shivam'],5,'2026-09-17','16:00','Technical Practice'],
['workshop','Workshop Technology','Mechanical','Technical Practice',['f-gaurav','f-shivam'],5,'2026-09-24','16:00','Technical Practice'],
['metrology','Metrology and Inspection','Mechanical','Technical Practice',['f-gaurav','f-shivam'],5,'2026-09-30','16:00','Technical Practice'],
['misc','Miscellaneous','Mechanical','Technical Practice',['f-gaurav','f-shivam'],5,'2026-10-03','12:15','Technical Practice'],
['automobile','Automobile Engineering','Mechanical','Technical Theory',['f-vivek'],10,'2026-10-26','12:30','Technical Theory C'],
['cadcam','CAD/CAM/CIM','Mechanical','Technical Theory',['f-vivek'],10,'2026-11-09','12:30','Technical Theory C'],
['computer','Computer','Non-Tech','Non-Technical Theory',['f-shivam'],10,'2026-10-22','11:15','Non-Tech Computer'],
['environment','Environment','Non-Tech','Non-Technical Theory',['f-anjna'],10,'2026-10-15','10:15','Non-Tech Environment']
].map((r,i)=>({id:'s-'+r[0],name:r[1],branch:r[2],category:r[3],facultyIds:r[4],classes:r[5],startDate:r[6],time:r[7],track:r[8],duration:60,order:i+1}));
const PDF_DEFAULT_IDS=['s-quant','s-reasoning','s-history','s-polity','s-geography','s-chemistry','s-biology','s-economy','s-physics','s-static-gk','s-nontech-practice'];
let state=load(),draft=blankDraft(),editingSubject=null,editingFaculty=null;
function load(){try{let x=JSON.parse(localStorage.getItem(KEY)||'{}');if(x&&x.version===2)return x}catch(e){}let plans=[];let p=makePlan(defaultDraft());p.id='plan-default-rrb-je-cbt2';p.batchName='RRB JE CBT 2 Mechanical Engineering 2026-27 | Question Practice + Theory Live Batch';p.examName='RRB JE 2026';p.status='Published';plans.push(p);return{version:2,faculty:[...FACULTY],subjects:[...SUBJECTS],plans};}
function save(){localStorage.setItem(KEY,JSON.stringify(state));toast('Saved');}
function blankDraft(){return{examName:'RRB JE 2026',batchName:'RRB JE CBT 2 Mechanical Engineering 2026-27 | Question Practice + Theory Live Batch',branch:'Mechanical',planType:'Theory + Question Practice',startDate:'2026-09-11',selectedFacultyIds:[],modules:[]};}
function defaultDraft(){let d=blankDraft();d.selectedFacultyIds=['f-gaurav','f-shivam','f-amrit','f-anjna','f-joshit'];d.modules=PDF_DEFAULT_IDS.map((id,i)=>moduleFromSubject(sub(id),{priority:i+1}));return d;}
function sub(id){return state.subjects.find(s=>s.id===id)||SUBJECTS.find(s=>s.id===id)} function fac(id){return state.faculty.find(f=>f.id===id)||FACULTY.find(f=>f.id===id)||{name:id}}
function uid(p){return p+'-'+Date.now().toString(36)+'-'+Math.random().toString(36).slice(2,7)} function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function pretty(iso){if(!iso)return'-';let [y,m,d]=iso.split('-').map(Number);return new Date(y,m-1,d).toLocaleDateString('en-GB',{day:'2-digit',month:'short',year:'numeric'})} function day(iso){let [y,m,d]=iso.split('-').map(Number);return new Date(y,m-1,d).toLocaleDateString('en-GB',{weekday:'short'})}
function addDays(iso,n){let [y,m,d]=iso.split('-').map(Number),dt=new Date(y,m-1,d);dt.setDate(dt.getDate()+n);return dt.toISOString().slice(0,10)} function isWork(iso){let [y,m,d]=iso.split('-').map(Number);return [1,2,3,4,5].includes(new Date(y,m-1,d).getDay())} function dates(start,count){let a=[],c=start,g=0;while(a.length<count&&g++<600){if(isWork(c))a.push(c);c=addDays(c,1)}return a}
function facultyNames(ids){return [...new Set(ids||[])].map(id=>fac(id).name).join(' & ')} function min(t){let[a,b]=String(t||'00:00').split(':').map(Number);return a*60+(b||0)}
function moduleFromSubject(s,o={}){return{id:o.id||uid('mod'),subjectId:s.id,subjectName:s.name,category:o.category||s.category,facultyIds:[...(o.facultyIds||s.facultyIds)],classes:+(o.classes||s.classes||1),priority:+(o.priority||s.order||1),track:o.track||s.track||'Track A',startMode:'custom',customStart:o.customStart||s.startDate,startTime:o.startTime||s.time,duration:+(o.duration||s.duration||60)}}
function cleanModules(mods){return(mods||[]).filter(m=>m&&m.subjectId).map(m=>({...m,facultyIds:[...(m.facultyIds||[])],classes:+m.classes||1,duration:+m.duration||60,priority:+m.priority||1,customStart:m.customStart||m.startDate||draft.startDate,startTime:m.startTime||m.time||'10:00'}))}
function sessions(mods){let out=[];cleanModules(mods).forEach(m=>{dates(m.customStart,m.classes).forEach((date,i)=>out.push({moduleId:m.id,subjectId:m.subjectId,subjectName:m.subjectName,topic:m.subjectName+' - '+(i+1),category:m.category,facultyIds:m.facultyIds,time:m.startTime,date,day:day(date),n:i+1}))});return out.sort((a,b)=>a.date.localeCompare(b.date)||min(a.time)-min(b.time)||a.subjectName.localeCompare(b.subjectName))}
function summary(mods){let ss=sessions(mods),uniq=[...new Set(ss.map(s=>s.date))];return{total:ss.length,tech:ss.filter(s=>s.category.includes('Technical')&&!s.category.includes('Non-')).length,non:ss.filter(s=>s.category.includes('Non-')).length,active:uniq.length,start:ss[0]?.date||'',end:ss.at(-1)?.date||'',fac:[...new Set(mods.flatMap(m=>m.facultyIds||[]))].length}}
function makePlan(d){let mods=cleanModules(d.modules),sum=summary(mods);return{id:uid('plan'),examName:d.examName,batchName:d.batchName,branch:d.branch,planType:d.planType,startDate:sum.start||d.startDate,endDate:sum.end,status:'Draft',modules:mods,sessions:sessions(mods),createdAt:new Date().toISOString()}}
function toast(msg){let t=document.querySelector('.toast')||document.body.appendChild(Object.assign(document.createElement('div'),{className:'toast'}));t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1800)}
const nav=[['/','Dashboard'],['/create','Create Study Plan'],['/plans','Study Plans'],['/templates','Templates'],['/faculty','Faculty'],['/subjects','Subjects'],['/calendar','Calendar'],['/settings','Settings']];
function shell(title,sub,body){let h=location.hash||'#/';return`<aside class="sidebar"><div class="brand"><img src="${LOGO}"><b>Examon Education</b></div>${nav.map(n=>`<a class="${h==='#'+n[0]?'active':''}" href="#${n[0]}">○ ${n[1]}</a>`).join('')}<div class="local-note"><b>Local App</b><br>Data saves in this browser.</div></aside><main class="main"><header class="topbar"><div><h1>${title}</h1><p>${sub}</p></div><div><a class="btn primary" href="#/create">+ Create</a><img src="${LOGO}"></div></header><section class="content">${body}</section></main><div class="toast"></div>`}
function dashboard(){let cards=state.plans.slice(-4).reverse().map(p=>`<div class="card"><h3>${esc(p.batchName)}</h3><p>${pretty(p.startDate)} → ${pretty(p.endDate)} · ${p.sessions.length} sessions</p><a class="btn primary" href="#/plans/${p.id}">Open</a></div>`).join('');return shell('Dashboard','Academic planner overview',`<div class="hero-card"><h2>Examon Academic Planner</h2><p>Study plan generator with reusable scheduler, faculty allocation and branded PDF.</p></div><div class="grid2">${cards||'<div class="card">No plans yet.</div>'}</div>`)}
function createPage(){let selected=new Set(draft.modules.map(m=>m.subjectId));let subjects=state.subjects.slice().sort((a,b)=>a.order-b.order).map(s=>`<label class="subject-card ${selected.has(s.id)?'selected':''}"><input type="checkbox" data-subject-check="${s.id}" ${selected.has(s.id)?'checked':''}><b>${esc(s.name)}</b><small>${s.branch} · ${s.category}</small><em>${facultyNames(s.facultyIds)}</em><span>${pretty(s.startDate)} · ${s.time} · ${s.classes} classes</span></label>`).join('');let facs=state.faculty.map(f=>`<label class="faculty-card ${draft.selectedFacultyIds.includes(f.id)?'selected':''}"><input type="checkbox" data-faculty-check="${f.id}" ${draft.selectedFacultyIds.includes(f.id)?'checked':''}><span class="avatar">${esc(f.name.split(' ').map(x=>x[0]).join('').slice(0,2))}</span><b>${esc(f.name)}</b><small>${esc(f.domain)}</small></label>`).join('');let mods=draft.modules.map(moduleCard).join('')||'<div class="empty">Select subjects above. Only selected subjects will appear here.</div>';return shell('Create Study Plan','Choose type, faculty, subjects, priority, dates and timings',`<div class="card soft"><div class="card-title"><h2>Start from an Existing Batch</h2><span>Import only old scheduler</span></div><div class="inline-row"><select id="import-plan">${state.plans.map(p=>`<option value="${p.id}">${esc(p.batchName)}</option>`).join('')}</select><button class="btn gold" id="import-scheduler">Import Scheduler Only</button><button class="btn primary" id="load-pdf-default">Load Attached Planner Default</button></div></div><div class="card"><div class="card-title"><h2>Batch Details</h2><span>You can change anytime</span></div><div class="grid3"><label>Exam Name<input id="d-exam" value="${esc(draft.examName)}"></label><label>Batch Name<input id="d-batch" value="${esc(draft.batchName)}"></label><label>Start Date<input id="d-start" type="date" value="${draft.startDate}"></label><label>Branch<select id="d-branch"><option ${draft.branch==='Mechanical'?'selected':''}>Mechanical</option><option ${draft.branch==='Civil'?'selected':''}>Civil</option><option ${draft.branch==='Non-Tech'?'selected':''}>Non-Tech</option></select></label><label>Plan Type<input id="d-type" value="${esc(draft.planType)}"></label></div></div><div class="card"><div class="card-title"><h2>Faculty Selection</h2><span>Select faculty. It will not auto-add scheduler.</span></div><div class="faculty-grid">${facs}</div><button class="btn primary" id="add-faculty">+ Add Faculty</button></div><div class="card"><div class="card-title"><h2>Subject Selection</h2><span>Untick removes from scheduler</span></div><div class="subject-grid compact-subjects">${subjects}</div><button class="btn primary" id="add-subject">+ Add Subject</button></div><div class="card"><div class="card-title"><h2>Scheduler</h2><span>Prefilled from sheet/PDF; edit if required</span></div><div id="scheduler-list">${mods}</div><div class="sticky-actions"><button class="btn" id="reset-draft">Reset</button><button class="btn primary" id="generate-plan">Generate Plan</button></div></div>`)}
function moduleCard(m){return`<div class="module-card" data-mid="${m.id}"><button class="remove" data-remove-module="${m.id}">Remove</button><span class="pill">${esc(m.category)}</span><h3>${esc(m.subjectName)}</h3><div class="assigned">ASSIGNED FACULTY <b>${facultyNames(m.facultyIds)}</b></div><div class="faculty-pills">${state.faculty.map(f=>`<label><input type="checkbox" data-module-faculty="${m.id}|${f.id}" ${m.facultyIds.includes(f.id)?'checked':''}>${esc(f.name)}</label>`).join('')}</div><div class="grid4"><label>Class Time<input data-mod-field="${m.id}|startTime" type="time" value="${m.startTime}"></label><label>Number of Classes<input data-mod-field="${m.id}|classes" type="number" min="1" value="${m.classes}"></label><label>Priority<input data-mod-field="${m.id}|priority" type="number" min="1" value="${m.priority}"></label><label>Track<input data-mod-field="${m.id}|track" value="${esc(m.track)}"></label><label>Category<input data-mod-field="${m.id}|category" value="${esc(m.category)}"></label><label>Duration<input data-mod-field="${m.id}|duration" type="number" value="${m.duration}"></label><label>Custom Start<input data-mod-field="${m.id}|customStart" type="date" value="${m.customStart}"></label></div></div>`}
function plansPage(){return shell('Study Plans','Saved academic roadmaps',`<div class="card"><table><thead><tr><th>Batch</th><th>Dates</th><th>Sessions</th><th>Actions</th></tr></thead><tbody>${state.plans.map(p=>`<tr><td><b>${esc(p.batchName)}</b><br><span>${p.examName} · ${p.branch} · ${p.planType}</span></td><td>${pretty(p.startDate)} → ${pretty(p.endDate)}</td><td>${p.sessions.length}</td><td><a class="btn primary" href="#/plans/${p.id}">Open</a> <button class="btn danger" data-delete-plan="${p.id}">Delete</button></td></tr>`).join('')}</tbody></table></div>`)}
function planDetail(id){let p=state.plans.find(x=>x.id===id);if(!p)return plansPage();let sum=summary(p.modules);return shell('Study Plan Detail',`${pretty(p.startDate)} → ${pretty(p.endDate)}`,`<div class="plan-head"><div><h2>${esc(p.batchName)}</h2><p>${p.examName} · ${p.branch} · ${p.planType}</p></div><div><button class="btn primary" data-edit-plan="${p.id}">Edit in Creator</button> <a class="btn gold" href="#/print/${p.id}">Print / Save PDF</a></div></div><div class="kpis"><div><span>Total Sessions</span><b>${sum.total}</b></div><div><span>Technical</span><b>${sum.tech}</b></div><div><span>Non-Technical</span><b>${sum.non}</b></div><div><span>Active Dates</span><b>${sum.active}</b></div></div><div class="card"><h2>Module Summary</h2>${moduleTable(p.modules)}</div>`)}
function moduleTable(mods){return`<table><thead><tr><th>Subject</th><th>Faculty</th><th>Classes</th><th>Time</th><th>Start Date</th></tr></thead><tbody>${mods.map(m=>`<tr><td><b>${esc(m.subjectName)}</b><br><span>${m.category}</span></td><td>${facultyNames(m.facultyIds)}</td><td>${m.classes}</td><td>${m.startTime}</td><td>${pretty(m.customStart)}</td></tr>`).join('')}</tbody></table>`}
function calendarPage(){let all=state.plans.flatMap(p=>(p.sessions||[]).map(s=>({...s,batch:p.batchName}))).sort((a,b)=>a.date.localeCompare(b.date)||min(a.time)-min(b.time));let groups={};all.forEach(s=>(groups[s.date]??=[]).push(s));let body=Object.keys(groups).map(d=>`<div class="day-card"><div class="day-head"><div><h2>${pretty(d)}</h2><p>${day(d)} · ${groups[d].length} class(es)</p></div><div><button class="btn" data-day-csv="${d}">Download Day CSV</button> <button class="btn gold" data-day-pdf="${d}">Download Day PDF</button></div></div>${groups[d].map(s=>`<div class="day-row"><b>${s.time}</b><div><strong>${esc(s.subjectName)}</strong><small>${esc(s.batch)}</small></div><div>${facultyNames(s.facultyIds)}</div><em>${esc(s.category)}</em></div>`).join('')}</div>`).join('');return shell('Calendar','Daily class calendar and exports',`<div class="card-title"><div><h2 class="big-title">Master Calendar</h2><p>Daily operations view across all batches. Total ${all.length} classes.</p></div><div><button class="btn" id="full-csv">Download Full CSV</button> <button class="btn gold" id="full-pdf">Download Full PDF</button></div></div>${body||'<div class="card empty">No sessions yet.</div>'}`)}
function facultyPage(){return shell('Faculty','Faculty master and subject association',`<div class="card"><div class="card-title"><h2>Faculty Master</h2><button class="btn primary" id="add-faculty">+ Add Faculty</button></div><div class="faculty-grid">${state.faculty.map(f=>`<div class="faculty-card static"><span class="avatar">${esc(f.name.split(' ').map(x=>x[0]).join('').slice(0,2))}</span><b>${esc(f.name)}</b><small>${esc(f.domain)}</small><button class="btn" data-edit-faculty="${f.id}">Edit</button></div>`).join('')}</div></div>`)}
function subjectsPage(){return shell('Subjects','Subject master and default scheduler data',`<div class="card"><div class="card-title"><div><h2>Subject Master</h2><p>Single subjects only. Defaults feed the scheduler.</p></div><button class="btn primary" id="add-subject">+ Add Subject</button></div><table><thead><tr><th>Subject</th><th>Branch</th><th>Category</th><th>Faculty</th><th>Classes</th><th>Start Date</th><th>Time</th><th></th></tr></thead><tbody>${state.subjects.map(s=>`<tr><td><b>${esc(s.name)}</b></td><td>${s.branch}</td><td>${s.category}</td><td>${facultyNames(s.facultyIds)}</td><td>${s.classes}</td><td>${pretty(s.startDate)}</td><td>${s.time}</td><td><button class="btn" data-edit-subject="${s.id}">Edit</button></td></tr>`).join('')}</tbody></table></div>`)}
function templatesPage(){return shell('Templates','Reusable structures',`<div class="card"><h2>RRB JE CBT 2 Mechanical Default</h2><p>Matches attached planner: 11 modules, 130 classes, 36 active dates.</p><button class="btn primary" id="load-pdf-default">Load in Creator</button></div>`)} function settingsPage(){return shell('Settings','App settings',`<div class="card"><h2>Settings</h2><p>Logo and footer are fixed for Examon branded PDF.</p></div>`)}
function printPage(id){let p=state.plans.find(x=>x.id===id)||state.plans[0],sum=summary(p.modules),ss=p.sessions||sessions(p.modules);let chunks=[];for(let i=0;i<ss.length;i+=24)chunks.push(ss.slice(i,i+24));let modulesByCat={};p.modules.forEach(m=>(modulesByCat[m.category]??=[]).push(m));let pages=`${printCover(p,sum)}${printOverview(p,sum)}${Object.keys(modulesByCat).map(c=>printModules(c,modulesByCat[c])).join('')}${chunks.map((c,i)=>printSchedule(c,i,ss.length)).join('')}${printNotes(p)}`;return`<div class="print-toolbar"><button onclick="history.back()" class="btn">← Back</button><button onclick="print()" class="btn gold">Print / Save as PDF</button></div><div class="print-area">${pages}</div>`}
function page(inner,foot='Study Plan'){return`<section class="print-page"><div class="sidebar-line"></div>${inner}<img class="watermark" src="${LOGO}"><footer><b>Examon Education | Mentorship: 8368886452</b><span>${foot}</span></footer></section>`}
function pHead(label){return`<div class="print-top"><b>${label}</b><img src="${LOGO}"></div>`}
function printCover(p,sum){return page(`${pHead((p.examName+' · '+p.branch).toUpperCase())}<div class="print-kicker">EXAMON EDUCATION · ACADEMIC ROADMAP</div><span class="pill">${p.planType}</span><h1>${esc(p.batchName)}</h1><p>Structured study plan with faculty allocation, module sequencing and date-wise class execution.</p><div class="gradient-line"></div><div class="print-kpis"><div><span>START DATE</span><b>${pretty(sum.start)}</b></div><div><span>END DATE</span><b>${pretty(sum.end)}</b></div><div><span>TOTAL CLASSES</span><b>${sum.total}</b></div><div><span>MODULES</span><b>${p.modules.length}</b></div></div><div class="dark-strip">${sum.tech} Technical&nbsp;&nbsp; | &nbsp;&nbsp;${sum.non} Non-Technical&nbsp;&nbsp; | &nbsp;&nbsp;${sum.active} Active Dates&nbsp;&nbsp; | &nbsp;&nbsp;${sum.fac} Faculties</div>`,'Study Plan · Cover')}
function printOverview(p,sum){let loads={};(p.modules||[]).forEach(m=>(m.facultyIds||[]).forEach(f=>loads[f]=(loads[f]||0)+(+m.classes||0)));return page(`${pHead('STUDY PLAN OVERVIEW')}<h2>Plan at a Glance</h2><p>A compact academic execution summary for the complete batch.</p><div class="print-kpis small"><div><b>${sum.total}</b><span>Total Classes</span></div><div><b>${sum.tech}</b><span>Technical</span></div><div><b>${sum.non}</b><span>Non-Technical</span></div><div><b>${sum.active}</b><span>Active Dates</span></div></div><div class="print-two"><div><h3>Faculty Load</h3>${Object.keys(loads).map(id=>`<p><b>${facultyNameLine(id)}</b><span>${loads[id]} classes</span></p>`).join('')}</div><div><h3>Academic Structure</h3><p><b>Batch Type</b><span>${p.planType}</span></p><p><b>Branch</b><span>${p.branch}</span></p><p><b>Working Days</b><span>Mon–Fri</span></p><p><b>Weekend Policy</b><span>Reschedule Only</span></p></div></div><h3>Module Summary</h3>${moduleTable(p.modules)}`,'Study Plan · Overview')}
function facultyNameLine(id){return fac(id).name}
function printModules(cat,mods){return page(`${pHead(cat.toUpperCase())}<span class="pill">${cat}</span><h2>Module Roadmap</h2><p>Subject-wise faculty, class count, timing and planned date window.</p><div class="roadmap">${mods.map((m,i)=>{let ds=dates(m.customStart,m.classes);return`<div class="road-card"><small>P${i+1}</small><h3>${esc(m.subjectName)}</h3><p>${facultyNames(m.facultyIds)}</p><div><span>CLASSES<br><b>${m.classes}</b></span><span>TIME<br><b>${m.startTime}</b></span></div><footer>${pretty(ds[0])} → ${pretty(ds.at(-1))}</footer></div>`}).join('')}</div>`,'Study Plan · Modules')}
function printSchedule(rows,i,total){return page(`${pHead('DATE-WISE EXECUTION PLAN')}<h2>Date-wise Schedule</h2><p>Sessions ${i*24+1}–${i*24+rows.length} of ${total}</p><table class="schedule-table"><thead><tr><th>#</th><th>Date</th><th>Day</th><th>Category</th><th>Subject / Topic</th><th>Faculty</th><th>Time</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${r.n}</td><td>${pretty(r.date)}</td><td>${r.day}</td><td>${r.category}</td><td><b>${esc(r.subjectName)}</b><br><span>${esc(r.topic)}</span></td><td>${facultyNames(r.facultyIds)}</td><td><b>${r.time}</b></td></tr>`).join('')}</tbody></table>`,'Study Plan · Schedule')}
function printNotes(p){return page(`${pHead('ACADEMIC NOTES')}<h2>Execution Notes</h2><div class="notes-grid"><div><b>01 Working Days</b><p>Regular classes are planned Monday to Friday. Saturday and Sunday remain off by default.</p></div><div><b>02 Rescheduling</b><p>Weekend sessions are used only through an explicit manual reschedule or approved academic exception.</p></div><div><b>03 Sequence</b><p>Priority controls the subject sequence inside a track. Different tracks can run in parallel.</p></div><div><b>04 Updates</b><p>This roadmap can be revised based on examination timelines, academic requirements or faculty availability.</p></div></div><div class="prepared"><b>Prepared for</b><h2>${esc(p.batchName)}</h2><p>${p.examName} · ${p.branch} · ${pretty(p.startDate)} to ${pretty(p.endDate)}</p></div>`,'Study Plan · Page')}
function render(){let h=(location.hash||'#/').slice(1),app=document.getElementById('app');if(h.startsWith('/plans/'))app.innerHTML=planDetail(h.split('/')[2]);else if(h==='/plans')app.innerHTML=plansPage();else if(h==='/create')app.innerHTML=createPage();else if(h==='/calendar')app.innerHTML=calendarPage();else if(h==='/faculty')app.innerHTML=facultyPage();else if(h==='/subjects')app.innerHTML=subjectsPage();else if(h==='/templates')app.innerHTML=templatesPage();else if(h==='/settings')app.innerHTML=settingsPage();else if(h.startsWith('/print/'))app.innerHTML=printPage(h.split('/')[2]);else app.innerHTML=dashboard();}
function syncDraft(){['exam','batch','start','branch','type'].forEach(k=>{let el=document.getElementById('d-'+k);if(el)draft[{exam:'examName',batch:'batchName',start:'startDate',branch:'branch',type:'planType'}[k]]=el.value})}
function modal(html){document.body.insertAdjacentHTML('beforeend',`<div class="modal-wrap"><div class="modal">${html}</div></div>`)}
function openFaculty(id){let f=id?state.faculty.find(x=>x.id===id):{id:'',name:'',domain:''};modal(`<h2>${id?'Edit':'Add'} Faculty</h2><label>Name<input id="mf-name" value="${esc(f.name)}"></label><label>Domain<input id="mf-domain" value="${esc(f.domain)}"></label><div class="modal-actions"><button class="btn" data-close>Cancel</button><button class="btn primary" id="save-faculty" data-id="${f.id}">Save</button></div>`)}
function openSubject(id){let s=id?state.subjects.find(x=>x.id===id):{id:'',name:'',branch:'Mechanical',category:'Technical Theory',facultyIds:[],classes:10,startDate:'2026-09-11',time:'12:30',track:'Track A',duration:60};modal(`<h2>${id?'Edit':'Add'} Subject</h2><div class="modal-scroll"><div class="grid2"><label>Subject<input id="ms-name" value="${esc(s.name)}"></label><label>Branch<input id="ms-branch" value="${esc(s.branch)}"></label><label>Category<input id="ms-category" value="${esc(s.category)}"></label><label>Classes<input id="ms-classes" type="number" value="${s.classes}"></label><label>Start Date<input id="ms-start" type="date" value="${s.startDate}"></label><label>Time<input id="ms-time" type="time" value="${s.time}"></label><label>Track<input id="ms-track" value="${esc(s.track)}"></label><label>Duration<input id="ms-duration" type="number" value="${s.duration}"></label></div><h3>Suggested Faculty</h3><div class="faculty-pills">${state.faculty.map(f=>`<label><input type="checkbox" data-ms-faculty="${f.id}" ${s.facultyIds.includes(f.id)?'checked':''}>${esc(f.name)}</label>`).join('')}</div></div><div class="modal-actions sticky-modal-actions"><button class="btn" data-close>Cancel</button><button class="btn primary" id="save-subject" data-id="${s.id}">Save</button></div>`)}
function exportCsv(rows,name){let csv=['Date,Day,Category,Subject,Topic,Faculty,Time'].concat(rows.map(r=>[pretty(r.date),r.day,r.category,r.subjectName,r.topic,facultyNames(r.facultyIds),r.time].map(x=>'"'+String(x).replace(/"/g,'""')+'"').join(','))).join('\n');let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download=name;a.click();URL.revokeObjectURL(a.href)}
document.addEventListener('change',e=>{let t=e.target;if(t.matches('[data-faculty-check]')){syncDraft();let id=t.dataset.facultyCheck;draft.selectedFacultyIds=t.checked?[...new Set([...draft.selectedFacultyIds,id])]:draft.selectedFacultyIds.filter(x=>x!==id);render()} if(t.matches('[data-subject-check]')){syncDraft();let s=sub(t.dataset.subjectCheck);if(t.checked&&!draft.modules.some(m=>m.subjectId===s.id))draft.modules.push(moduleFromSubject(s));if(!t.checked)draft.modules=draft.modules.filter(m=>m.subjectId!==s.id);render()} if(t.matches('[data-module-faculty]')){let[id,fid]=t.dataset.moduleFaculty.split('|'),m=draft.modules.find(x=>x.id===id);if(m){m.facultyIds=t.checked?[...new Set([...m.facultyIds,fid])]:m.facultyIds.filter(x=>x!==fid);render()}} if(t.matches('[data-mod-field]')){let[id,k]=t.dataset.modField.split('|'),m=draft.modules.find(x=>x.id===id);if(m)m[k]=['classes','priority','duration'].includes(k)?+t.value:t.value}});
document.addEventListener('click',e=>{let t=e.target;if(t.id==='import-scheduler'){syncDraft();let p=state.plans.find(x=>x.id===document.getElementById('import-plan').value);if(p){draft.modules=cleanModules(p.modules).map(m=>({...m,id:uid('mod')}));toast('Scheduler imported only');render()}} if(t.id==='load-pdf-default'){draft=defaultDraft();toast('Default planner loaded');location.hash='#/create';render()} if(t.dataset.removeModule){draft.modules=draft.modules.filter(m=>m.id!==t.dataset.removeModule);render()} if(t.id==='reset-draft'){draft=blankDraft();render()} if(t.id==='generate-plan'){syncDraft();if(!draft.modules.length)return toast('Select at least one subject');let p=makePlan(draft),i=state.plans.findIndex(x=>x.id===draft.id);if(i>=0)state.plans[i]=p;else state.plans.push(p);save();location.hash='#/plans/'+p.id} if(t.dataset.editPlan){let p=state.plans.find(x=>x.id===t.dataset.editPlan);if(p){draft={...blankDraft(),...p,modules:cleanModules(p.modules).map(m=>({...m,id:uid('mod')})),selectedFacultyIds:[...new Set(p.modules.flatMap(m=>m.facultyIds||[]))]};location.hash='#/create'}} if(t.dataset.deletePlan){state.plans=state.plans.filter(p=>p.id!==t.dataset.deletePlan);save();render()} if(t.id==='add-faculty')openFaculty(); if(t.dataset.editFaculty)openFaculty(t.dataset.editFaculty); if(t.id==='add-subject')openSubject(); if(t.dataset.editSubject)openSubject(t.dataset.editSubject); if(t.dataset.close||t.classList.contains('modal-wrap'))document.querySelector('.modal-wrap')?.remove(); if(t.id==='save-faculty'){let f={id:t.dataset.id||uid('f'),name:document.getElementById('mf-name').value,domain:document.getElementById('mf-domain').value};let i=state.faculty.findIndex(x=>x.id===f.id);i>=0?state.faculty[i]=f:state.faculty.push(f);document.querySelector('.modal-wrap')?.remove();save();render()} if(t.id==='save-subject'){let ids=[...document.querySelectorAll('[data-ms-faculty]:checked')].map(x=>x.dataset.msFaculty);let s={id:t.dataset.id||'s-'+uid('custom'),name:document.getElementById('ms-name').value,branch:document.getElementById('ms-branch').value,category:document.getElementById('ms-category').value,facultyIds:ids,classes:+document.getElementById('ms-classes').value,startDate:document.getElementById('ms-start').value,time:document.getElementById('ms-time').value,track:document.getElementById('ms-track').value,duration:+document.getElementById('ms-duration').value,order:state.subjects.length+1};let i=state.subjects.findIndex(x=>x.id===s.id);i>=0?state.subjects[i]=s:state.subjects.push(s);document.querySelector('.modal-wrap')?.remove();save();render()} if(t.id==='full-csv')exportCsv(state.plans.flatMap(p=>p.sessions||[]),'full-calendar.csv'); if(t.dataset.dayCsv){let rows=state.plans.flatMap(p=>p.sessions||[]).filter(s=>s.date===t.dataset.dayCsv);exportCsv(rows,'calendar-'+t.dataset.dayCsv+'.csv')} if(t.id==='full-pdf')print(); if(t.dataset.dayPdf)print();});
window.addEventListener('hashchange',render);render();
})();