/div></div>${Object.keys(grouped).length?Object.entries(grouped).map(([d,list])=>`<div class="day-group"><div class="day-head"><div><b>${pretty(d)}</b><span class="day-sub">${dayName(d)} · ${list.length} class(es)</span></div><div class="toolbar"><button class="btn small" onclick="exportDayCSV('${d}')">Export Day CSV</button><button class="btn small gold" onclick="printDay('${d}')">Print / PDF Day</button></div></div>${list.map(x=>`<div class="session"><div class="time">${x.startTime}</div><div><b>${esc(x.subjectName)}</b><div class="meta"><a href="#/plans/${x.planId}">${esc(x.batch)}</a></div></div><div class="faculty">${facultyNames(x.facultyIds)}</div><div class="meta">${x.category}</div>${x.exception?'<span class="tag gold">Weekend</span>':'<span></span>'}</div>`).join('')}</div>`).join(''):'<div class="card empty">No scheduled sessions yet.</div>'}`,'Calendar','Daily class calendar and exports')}
function csvDownload(name,rows){const csv=rows.map(r=>r.map(v=>`"${String(v??'').replaceAll('\"','\"\"')}"`).join(',')).join('\n');const blob=new Blob([csv],{type:'text/csv;charset=utf-8'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;a.click();URL.revokeObjectURL(a.href)}
function dayRows(date){const rows=[['Date','Day','Time','Batch','Exam','Subject','Category','Faculty']];state.plans.forEach(p=>(p.sessions||[]).filter(s=>s.date===date).sort((a,b)=>a.startTime.localeCompare(b.startTime)).forEach(s=>rows.push([date,dayName(date),s.startTime,p.batchName,p.examName,s.subjectName,s.category,facultyNames(s.facultyIds)])));return rows}
function exportDayCSV(date){csvDownload(`examon-calendar-${date}.csv`,dayRows(date))}
function exportCalendarCSV(){const rows=[['Date','Day','Time','Batch','Exam','Subject','Category','Faculty']];state.plans.forEach(p=>(p.sessions||[]).forEach(s=>rows.push([s.date,dayName(s.date),s.startTime,p.batchName,p.examName,s.subjectName,s.category,facultyNames(s.facultyIds)])));rows.splice(1,rows.length-1,...rows.slice(1).sort((a,b)=>a[0].localeCompare(b[0])||a[2].localeCompare(b[2])));csvDownload('examon-master-calendar.csv',rows)}
function printDay(date){const rows=dayRows(date).slice(1);const w=window.open('','_blank');w.document.write(`<!doctype html><html><head><title>Examon Daily Calendar ${date}</title><style>body{font-family:Arial;color:#14233a;padding:28px}header{display:flex;justify-content:space-between;align-items:center;border-bottom:4px solid #20bfc7;padding-bottom:14px;margin-bottom:20px}img{height:62px}h1{margin:0}table{width:100%;border-collapse:collapse}th,td{border:1px solid #d5dde5;padding:9px;font-size:12px}th{background:#edf8f8}.meta{color:#5d7083}</style></head><body><header><div><h1>Daily Class Calendar</h1><div class="meta">${pretty(date)} · ${dayName(date)} · ${rows.length} classes</div></div><img src="${state.settings.logo}"></header><table><thead><tr>${dayRows(date)[0].slice(2).map(x=>`<th>${x}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${r.slice(2).map(x=>`<td>${esc(x)}</td>`).join('')}</tr>`).join('')}</tbody></table><script>window.onload=()=>window.print()<\/script></body></html>`);w.document.close()}

function settingsPage(){return layout(`<div class="page-title"><div><h2>Settings</h2><p>Branding, working-day defaults and data backup.</p></div></div><div class="grid grid-2"><div class="card pad"><h3>Company & Branding</h3><div class="field"><label>Company Name</label><input id="set-company" value="${esc(state.settings.company)}"></div><div class="field" style="margin-top:10px"><label>Footer Text</label><input id="set-footer" value="${esc(state.settings.footer)}"></div><div style="margin-top:12px"><img src="${state.settings.logo}" style="max-height:120px;max-width:100%;object-fit:contain"></div><button class="btn primary" id="save-settings" style="margin-top:12px">Save Settings</button></div><div class="card pad"><h3>Scheduling Defaults</h3><div class="alert cyan"><b>Default working days:</b> Monday–Friday.<br>Saturday and Sunday stay off unless you explicitly reschedule or enable a module override.</div><div class="field" style="margin-top:12px"><label>Blocked / Holiday Dates (one per line, YYYY-MM-DD)</label><textarea id="set-holidays" rows="8">${esc(state.settings.holidays.join('\n'))}</textarea></div></div><div class="card pad"><h3>Data Backup</h3><p class="small-muted">Planner data is stored in LocalStorage in this browser.</p><div class="toolbar"><button class="btn" id="export-data">Export Data</button><label class="btn">Import Data<input id="import-data" type="file" accept="application/json" hidden></label><button class="btn red" id="reset-data">Reset App</button></div></div><div class="card pad"><h3>Current Rules</h3><div class="small-muted">• User enters date and time.<br>• Subject priority controls order within each track.<br>• Parallel tracks can run together.<br>• Saturday/Sunday are not auto-generated.<br>• Manual weekend rescheduling is allowed.<br>• Faculty conflicts are detected.</div></div></div>`,'Settings','Branding, default working days and data management')}

function printPage(id){
  const p=state.plans.find(x=>x.id===id);
  if(!p)return '<div class="print-area"><h2>Plan not found.</h2></div>';
  const s=summary(p), pages=[];
  const range=m=>{const a=p.sessions.filter(x=>x.moduleId===m.id).map(x=>x.date).sort();return {start:a[0]||'',end:a[a.length-1]||''}};
  const catClass=c=>c.includes('Technical')&&!c.includes('Non-Technical')?'tech':c.includes('Non-Technical')?'nontech':c.includes('Practice')?'practice':'other';
  const moduleGroups={}; p.modules.forEach(m=>(moduleGroups[m.category]??=[]).push(m));
  const facultyLoad=state.faculty.map(f=>({f,n:p.sessions.filter(x=>x.facultyIds.includes(f.id)).length})).filter(x=>x.n).sort((a,b)=>b.n-a.n);
  const maxLoad=Math.max(1,...facultyLoad.map(x=>x.n));

  pages.push(`<section class="print-page cover-page">
    <div class="print-topline"><span>${esc(p.examName)} · ${esc(p.branch)}</span><img class="print-logo-inline" src="${state.settings.logo}"></div>
    <div class="cover-hero">
      <div class="cover-kicker">EXAMON EDUCATION · ACADEMIC ROADMAP</div>
      <span class="cover-badge">${esc(p.planType)}</span>
      <h1>${esc(p.batchName)}</h1>
      <p class="cover-desc">Structured study plan with faculty allocation, module sequencing and date-wise class execution.</p>
      <div class="cover-band"></div>
    </div>
    <div class="cover-meta premium">
      <div><span>START DATE</span><b>${pretty(s.start)}</b></div>
      <div><span>END DATE</span><b>${pretty(s.end)}</b></div>
      <div><span>TOTAL CLASSES</span><b>${s.total}</b></div>
      <div><span>MODULES</span><b>${s.modules}</b></div>
    </div>
    <div class="cover-summary-strip"><span>${s.technical} Technical</span><i></i><span>${s.nontech} Non-Technical</span><i></i><span>${s.active} Active Dates</span><i></i><span>${facultyLoad.length} Faculties</span></div>
    <div class="print-footer"><span>${esc(state.settings.footer)}</span><span>Study Plan · Cover</span></div>
  </section>`);

  pages.push(`<section class="print-page overview-page">
    <div class="print-topline"><span>STUDY PLAN OVERVIEW</span><img class="print-logo-inline" src="${state.settings.logo}"></div>
    <h2>Plan at a Glance</h2>
    <p class="section-lead">A compact academic execution summary for the complete batch.</p>
    <div class="pkpis premium-kpis">
      <div class="pkpi"><b>${s.total}</b><span>Total Classes</span></div>
      <div class="pkpi"><b>${s.technical}</b><span>Technical</span></div>
      <div class="pkpi"><b>${s.nontech}</b><span>Non-Technical</span></div>
      <div class="pkpi"><b>${s.active}</b><span>Active Dates</span></div>
    </div>
    <div class="overview-grid">
      <div class="overview-panel"><h3>Faculty Load</h3>${facultyLoad.map(x=>`<div class="load-row"><div><b>${esc(x.f.name)}</b><span>${x.n} classes</span></div><div class="load-track"><i style="width:${Math.max(8,Math.round(x.n/maxLoad*100))}%"></i></div></div>`).join('')}</div>
      <div class="overview-panel"><h3>Academic Structure</h3>
        <div class="structure-list"><div><span>Batch Type</span><b>${esc(p.planType)}</b></div><div><span>Branch</span><b>${esc(p.branch)}</b></div><div><span>Working Days</span><b>Mon–Fri</b></div><div><span>Weekend Policy</span><b>Reschedule Only</b></div><div><span>Tracks</span><b>${new Set(p.modules.map(m=>m.track)).size}</b></div></div>
      </div>
    </div>
    <h3 class="section-title">Module Summary</h3>
    <table class="premium-table module-summary"><thead><tr><th>Subject</th><th>Faculty</th><th>Classes</th><th>Priority</th><th>Track</th><th>Time</th></tr></thead><tbody>${p.modules.map(m=>`<tr><td><b>${esc(m.subjectName)}</b><small>${esc(m.category)}</small></td><td>${facultyNames(m.facultyIds)}</td><td>${m.classes}</td><td>P${m.priority}</td><td>${esc(m.track)}</td><td>${m.startTime}</td></tr>`).join('')}</tbody></table>
    <div class="print-footer"><span>${esc(state.settings.footer)}</span><span>Overview</span></div>
  </section>`);

  let modulePageNo=1;
  Object.entries(moduleGroups).forEach(([cat,mods])=>{
    for(let i=0;i<mods.length;i+=6){const arr=mods.slice(i,i+6);pages.push(`<section class="print-page modules-page">
      <div class="print-topline"><span>${esc(cat.toUpperCase())}</span><img class="print-logo-inline" src="${state.settings.logo}"></div>
      <div class="section-heading-row"><div><span class="category-pill ${catClass(cat)}">${esc(cat)}</span><h2>Module Roadmap</h2><p class="section-lead">Subject-wise faculty, class count, timing and planned date window.</p></div><div class="module-page-count">${arr.length} module${arr.length!==1?'s':''}</div></div>
      <div class="module-card-grid">${arr.map(m=>{const r=range(m);return `<article class="module-card ${catClass(m.category)}"><div class="module-card-top"><span>${esc(m.track)}</span><b>P${m.priority}</b></div><h3>${esc(m.subjectName)}</h3><div class="module-faculty">${facultyNames(m.facultyIds)}</div><div class="module-stats"><div><span>CLASSES</span><b>${m.classes}</b></div><div><span>TIME</span><b>${m.startTime}</b></div></div><div class="module-range"><span>${r.start?pretty(r.start):'—'}</span><i>→</i><span>${r.end?pretty(r.end):'—'}</span></div></article>`}).join('')}</div>
      <div class="print-footer"><span>${esc(state.settings.footer)}</span><span>Modules ${modulePageNo++}</span></div>
    </section>`)}
  });

  const chunk=24;
  for(let i=0;i<p.sessions.length;i+=chunk){const arr=p.sessions.slice(i,i+chunk);pages.push(`<section class="print-page schedule-page">
    <div class="print-topline"><span>DATE-WISE EXECUTION PLAN</span><img class="print-logo-inline" src="${state.settings.logo}"></div>
    <div class="schedule-heading"><div><h2>Date-wise Schedule</h2><p class="section-lead">Sessions ${i+1}–${i+arr.length} of ${p.sessions.length}</p></div><div class="schedule-range">${pretty(arr[0].date)}<span>to</span>${pretty(arr[arr.length-1].date)}</div></div>
    <table class="premium-table schedule-table"><thead><tr><th>#</th><th>Date</th><th>Day</th><th>Category</th><th>Subject / Topic</th><th>Faculty</th><th>Time</th></tr></thead><tbody>${arr.map((x,j)=>`<tr class="${j%2?'alt':''}"><td><span class="class-no">${x.classIndex}</span></td><td><b>${pretty(x.date)}</b></td><td>${dayName(x.date).slice(0,3)}</td><td><span class="row-pill ${catClass(x.category)}">${esc(x.category.replace('Non-Technical','Non-Tech'))}</span></td><td><b>${esc(x.subjectName)}</b><small>${esc(x.topic)}</small></td><td>${facultyNames(x.facultyIds)}</td><td><b>${x.startTime}</b></td></tr>`).join('')}</tbody></table>
    <div class="print-footer"><span>${esc(state.settings.footer)}</span><span>Schedule ${Math.floor(i/chunk)+1}</span></div>
  </section>`)}

  pages.push(`<section class="print-page notes-page">
    <div class="print-topline"><span>ACADEMIC NOTES</span><img class="print-logo-inline" src="${state.settings.logo}"></div>
    <h2>Execution Notes</h2><p class="section-lead">Operational guidance for the academic team and learners.</p>
    <div class="notes-grid">
      <div><b>01</b><h3>Working Days</h3><p>Regular classes are planned Monday to Friday. Saturday and Sunday remain off by default.</p></div>
      <div><b>02</b><h3>Rescheduling</h3><p>Weekend sessions are used only through an explicit manual reschedule or approved academic exception.</p></div>
      <div><b>03</b><h3>Sequence</h3><p>Priority controls the subject sequence inside a track. Different tracks can run in parallel.</p></div>
      <div><b>04</b><h3>Updates</h3><p>This roadmap can be revised based on examination timelines, academic requirements or faculty availability.</p></div>
    </div>
    <div class="notes-signoff"><span>Prepared for</span><b>${esc(p.batchName)}</b><small>${esc(p.examName)} · ${esc(p.branch)} · ${pretty(s.start)} to ${pretty(s.end)}</small></div>
    <div class="print-footer"><span>${esc(state.settings.footer)}</span><span>Academic Notes</span></div>
  </section>`);
  return `<div class="print-toolbar no-print"><button class="btn" onclick="history.back()">← Back</button><button class="btn gold" onclick="window.print()">Print / Save as PDF</button></div><div class="print-area">${pages.join('')}</div>`
}

function bindPage(r){
  if(r==='create'){
    $('#c-branch')?.addEventListener('change',()=>{syncDraft();draft.branch=$('#c-branch').value;draft.modules=[];render()});
    $$('.faculty-select-card').forEach(card=>card.addEventListener('click',()=>{syncDraft();const id=card.dataset.fid;const i=draft.selectedFacultyIds.indexOf(id);if(i>=0)draft.selectedFacultyIds.splice(i,1);else draft.selectedFacultyIds.push(id);draft.modules=draft.modules.filter(m=>{const s=state.subjects.find(x=>x.id===m.subjectId);return !draft.selectedFacultyIds.length || (s?.facultyIds||[]).some(fid=>draft.selectedFacultyIds.includes(fid))});draft.modules.forEach(m=>{const s=state.subjects.find(x=>x.id===m.subjectId);if(s)m.facultyIds=(s.facultyIds||[]).filter(fid=>draft.selectedFacultyIds.includes(fid))});render()}));
    $$('[data-subid]').forEach(x=>x.addEventListener('change',()=>{syncDraft();if(x.checked)addSubjectToDraft(x.dataset.subid);else draft.modules=draft.modules.filter(m=>m.subjectId!==x.dataset.subid);render()}));
    $('#subject-filter')?.addEventListener('change',e=>{$$('.subject-card').forEach(c=>c.style.display=e.target.value==='all'||c.dataset.branch===e.target.value?'block':'none')});
    $('#use-template-btn')?.addEventListener('click',()=>{syncDraft();openTemplatePicker()});
    $('#rolling-import')?.addEventListener('click',()=>{syncDraft();const id=$('#rolling-source')?.value;if(!id){toast('Choose a previous study plan');return}const src=state.plans.find(p=>p.id===id);if(!src)return;draft=importRollingPlan(src,draft.startDate);draft.batchName='';toast('Previous batch imported. Completed subjects moved to the end.');render()});
    $$('.del-module').forEach(b=>b.addEventListener('click',()=>{syncDraft();draft.modules.splice(+b.dataset.i,1);render()}));
    $$('.m-mode').forEach(sel=>sel.addEventListener('change',()=>{const el=sel.closest('[data-mi]');$('.m-custom',el).disabled=sel.value!=='custom'}));
    $('#reset-draft')?.addEventListener('click',()=>{if(confirm('Reset this draft?')){draft=blankDraft();render()}});
    $('#generate-plan')?.addEventListener('click',()=>{syncDraft();if(!draft.examName||!draft.batchName||!draft.startDate){toast('Exam, batch name and start date are required');return}if(!draft.modules.length){toast('Select at least one subject');return}const res=generatePlanSessions(draft);if(res.errors.length){toast(res.errors[0]);return}draft.sessions=res.sessions;draft.updatedAt=new Date().toISOString();const ix=state.plans.findIndex(p=>p.id===draft.id);if(ix>=0)state.plans[ix]=deep(draft);else state.plans.unshift(deep(draft));save();toast(`Generated ${res.sessions.length} sessions`);const id=draft.id;draft=null;go(`plans/${id}`)});
  }
  if(r==='plans'){$$('.delete-plan').forEach(b=>b.addEventListener('click',()=>{if(confirm('Delete this plan?')){state.plans=state.plans.filter(p=>p.id!==b.dataset.id);save();render()}}));$$('.clone-plan').forEach(b=>b.addEventListener('click',()=>{const p=deep(state.plans.find(x=>x.id===b.dataset.id));const name=prompt('New batch name',p.batchName+' Copy');if(!name)return;const start=prompt('New start date (YYYY-MM-DD)',p.startDate);if(!start)return;p.id=uid('plan');p.batchName=name;p.startDate=start;p.status='Draft';p.version=1;p.modules.forEach(m=>m.id=uid('mod'));const res=generatePlanSessions(p);p.sessions=res.sessions;state.plans.unshift(p);save();toast('Plan cloned');render()}));$$('.roll-plan').forEach(b=>b.addEventListener('click',()=>{const src=state.plans.find(x=>x.id===b.dataset.id);const start=prompt('New batch start date (YYYY-MM-DD)',iso(new Date()));if(!start)return;draft=importRollingPlan(src,start);draft.batchName=prompt('New batch name',src.batchName.replace(/(1\.0|2\.0|3\.0)/,'2.0')+' - New')||src.batchName+' - New';toast('Rolling batch prepared. Completed subjects moved to the end.');go('create')}))}
  if(r.startsWith('plans/')){const id=r.split('/')[1],p=state.plans.find(x=>x.id===id);$('#plan-status')?.addEventListener('change',e=>{p.status=e.target.value;p.version++;save();toast('Status updated')});$$('.tabs button').forEach(btn=>btn.addEventListener('click',()=>{$$('.tabs button').forEach(x=>x.classList.remove('active'));btn.classList.add('active');['modules','calendar','table'].forEach(t=>$('#tab-'+t).style.display=t===btn.dataset.tab?'block':'none')}));$$('.lock-session').forEach(x=>x.addEventListener('change',()=>{const s=p.sessions.find(y=>y.id===x.dataset.sid);s.locked=x.checked;save()}));$$('.resched').forEach(b=>b.addEventListener('click',()=>rescheduleModal(p,b.dataset.sid)));}
  if(r==='templates'){$$('.use-template').forEach(b=>b.addEventListener('click',()=>{const t=state.templates.find(x=>x.id===b.dataset.id);draft=blankDraft();applyTemplate(t);go('create')}))}
  if(r==='faculty'){$('#add-faculty')?.addEventListener('click',()=>facultyModal());$$('.edit-faculty').forEach(b=>b.addEventListener('click',()=>facultyModal(state.faculty.find(x=>x.id===b.dataset.id))))}
  if(r==='subjects'){$('#add-subject')?.addEventListener('click',()=>subjectModal());$$('.edit-subject').forEach(b=>b.addEventListener('click',()=>subjectModal(state.subjects.find(x=>x.id===b.dataset.id))))}
  if(r==='settings'){$('#save-settings')?.addEventListener('click',()=>{state.settings.company=$('#set-company').value;state.settings.footer=$('#set-footer').value;state.settings.holidays=$('#set-holidays').value.split(/\s+/).filter(Boolean);save();toast('Settings saved')});$('#export-data')?.addEventListener('click',()=>{const blob=new Blob([JSON.stringify(state,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='examon-planner-backup.json';a.click();URL.revokeObjectURL(a.href)});$('#import-data')?.addEventListener('change',e=>{const f=e.target.files[0];if(!f)return;const rd=new FileReader();rd.onload=()=>{try{state=JSON.parse(rd.result);save();toast('Data imported');render()}catch{alert('Invalid backup file')}};rd.readAsText(f)});$('#reset-data')?.addEventListener('click',()=>{if(confirm('Reset all planner data?')){state=seed();save();draft=null;render()}})}
}
function applyTemplate(t){draft.examName=t.exam;draft.branch=t.branch;draft.planType=t.type;draft.contentFlags=[...t.flags];draft.modules=t.modules.map(m=>({...deep(m),id:uid('mod'),customStart:'',startTime:m.startTime||''}));draft.selectedFacultyIds=[...new Set(draft.modules.flatMap(m=>m.facultyIds||[]))]}
function openTemplatePicker(){const m=document.createElement('div');m.className='modal-backdrop';m.innerHTML=`<div class="modal"><h3>Load Template</h3><div class="grid">${state.templates.map(t=>`<button class="btn block pick-template" data-id="${t.id}" style="justify-content:flex-start"><b>${esc(t.name)}</b>&nbsp; <span class="small-muted">${t.type} · ${t.branch}</span></button>`).join('')}</div><button class="btn" id="close-modal" style="margin-top:14px">Cancel</button></div>`;document.body.appendChild(m);$('#close-modal',m).onclick=()=>m.remove();$$('.pick-template',m).forEach(b=>b.onclick=()=>{syncDraft();applyTemplate(state.templates.find(x=>x.id===b.dataset.id));m.remove();render()})}
function rescheduleModal(p,sid){const s=p.sessions.find(x=>x.id===sid);const m=document.createElement('div');m.className='modal-backdrop';m.innerHTML=`<div class="modal"><h3>Reschedule Session</h3><div class="alert cyan"><b>${esc(s.subjectName)}</b><br>${pretty(s.date)} · ${s.startTime}</div><div class="form-grid" style="margin-top:14px"><div class="field span-6"><label>New Date</label><input id="rs-date" type="date" value="${s.date}"></div><div class="field span-6"><label>New Time</label><input id="rs-time" type="time" value="${s.startTime}"></div></div><div class="toolbar" style="margin-top:14px"><button class="btn" id="rs-next">Next Working Day</button><button class="btn gold" id="rs-save">Save Date</button><button class="btn" id="rs-close">Cancel</button></div><div class="small-muted" style="margin-top:10px">Saturday/Sunday are allowed only because this is a manual reschedule.</div></div>`;document.body.appendChild(m);$('#rs-close',m).onclick=()=>m.remove();$('#rs-next',m).onclick=()=>{$('#rs-date',m).value=nextWorking(s.date,p.workingDays,state.settings.holidays)};$('#rs-save',m).onclick=()=>{const nd=$('#rs-date',m).value;if(!nd)return;s.date=nd;s.startTime=$('#rs-time',m).value;s.exception=[0,6].includes(toDate(nd).getDay())?'Weekend':'Manual';p.sessions.sort((a,b)=>a.date.localeCompare(b.date)||a.startTime.localeCompare(b.startTime));p.version++;save();m.remove();toast('Session rescheduled');render()}}
function facultyModal(f=null){const x=f?deep(f):{id:uid('f'),name:'',designation:'Faculty',domains:[],subjects:[],active:true,weekend:true};const m=document.createElement('div');m.className='modal-backdrop';m.innerHTML=`<div class="modal"><h3>${f?'Edit':'Add'} Faculty</h3><div class="form-grid"><div class="field span-6"><label>Name</label><input id="fm-name" value="${esc(x.name)}"></div><div class="field span-6"><label>Designation</label><input id="fm-des" value="${esc(x.designation)}"></div><div class="field span-12"><label>Domains (comma separated)</label><input id="fm-dom" value="${esc(x.domains.join(', '))}"></div><div class="field span-12"><label>Subjects (comma separated)</label><textarea id="fm-sub" rows="4">${esc(x.subjects.join(', '))}</textarea></div><div class="field span-6"><label>Active</label><select id="fm-active"><option value="true" ${x.active?'selected':''}>Yes</option><option value="false" ${!x.active?'selected':''}>No</option></select></div><div class="field span-6"><label>Weekend reschedule allowed</label><select id="fm-week"><option value="true" ${x.weekend?'selected':''}>Yes</option><option value="false" ${!x.weekend?'selected':''}>No</option></select></div></div><div class="toolbar" style="margin-top:14px"><button class="btn primary" id="fm-save">Save</button><button class="btn" id="fm-close">Cancel</button></div></div>`;document.body.appendChild(m);$('#fm-close',m).onclick=()=>m.remove();$('#fm-save',m).onclick=()=>{x.name=$('#fm-name',m).value.trim();if(!x.name)return;x.designation=$('#fm-des',m).value;x.domains=$('#fm-dom',m).value.split(',').map(v=>v.trim()).filter(Boolean);x.subjects=$('#fm-sub',m).value.split(',').map(v=>v.trim()).filter(Boolean);x.active=$('#fm-active',m).value==='true';x.weekend=$('#fm-week',m).value==='true';const i=state.faculty.findIndex(y=>y.id===x.id);if(i>=0)state.faculty[i]=x;else state.faculty.push(x);save();m.remove();render();toast('Faculty saved')}}
function subjectModal(su=null){const x=su?deep(su):{id:uid('s'),name:'',branch:'Mechanical',technical:true,category:'Technical Theory',facultyIds:[],duration:60,active:true};const m=document.createElement('div');m.className='modal-backdrop';m.innerHTML=`<div class="modal"><h3>${su?'Edit':'Add'} Subject</h3><div class="form-grid"><div class="field span-6"><label>Name</label><input id="sm-name" value="${esc(x.name)}"></div><div class="field span-6"><label>Branch</label><select id="sm-branch">${BRANCHES.map(v=>`<option ${x.branch===v?'selected':''}>${v}</option>`).join('')}</select></div><div class="field span-6"><label>Category</label><select id="sm-cat">${CATEGORIES.map(v=>`<option ${x.category===v?'selected':''}>${v}</option>`).join('')}</select></div><div class="field span-6"><label>Duration (min)</label><input id="sm-duration" type="number" value="${x.duration}"></div><div class="field span-12"><label>Suggested Faculty</label><select id="sm-fac" multiple size="6">${state.faculty.map(f=>`<option value="${f.id}" ${x.facultyIds.includes(f.id)?'selected':''}>${esc(f.name)}</option>`).join('')}</select></div></div><div class="toolbar" style="margin-top:14px"><button class="btn primary" id="sm-save">Save</button><button class="btn" id="sm-close">Cancel</button></div></div>`;document.body.appendChild(m);$('#sm-close',m).onclick=()=>m.remove();$('#sm-save',m).onclick=()=>{x.name=$('#sm-name',m).value.trim();if(!x.name)return;x.branch=$('#sm-branch',m).value;x.category=$('#sm-cat',m).value;x.technical=x.category.startsWith('Technical');x.duration=+$(`#sm-duration`,m).value;x.facultyIds=$$('#sm-fac option:checked',m).map(o=>o.value);const i=state.subjects.findIndex(y=>y.id===x.id);if(i>=0)state.subjects[i]=x;else state.subjects.push(x);save();m.remove();render();toast('Subject saved')}}

window.go=go;